apiVersion: v1
kind: PersistentVolume
metadata:
  name: grafana-dev-pv
spec:
  capacity:
    storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-sc
  persistentVolumeReclaimPolicy: Retain
  claimRef:
    namespace: dev
    name: grafana-pvc
  csi:
    driver: ebs.csi.aws.com
    volumeHandle: vol-0c2eace0648046482
    fsType: ext4
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: topology.kubernetes.io/zone
              operator: In
              values:
                - us-east-1b
